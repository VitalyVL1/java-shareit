# java-shareit

**java-shareit** — это учебное приложение для шеринга вещей. Оно позволяет пользователям делиться своими вещами (инструментами, книгами, техникой и т.д.), брать их в аренду на определенные даты, а также оставлять запросы на вещи, которых у них пока нет.

Проект реализован в рамках учебной программы и демонстрирует навыки разработки на Java с использованием Spring Boot и модульной архитектуры.

## 📁 Структура проекта

Проект разделен на два основных модуля для разделения ответственности и упрощения валидации:

*   **`gateway`** — модуль-шлюз, который принимает все входящие HTTP-запросы от клиентов, выполняет первичную валидацию данных (формат, обязательные поля) и перенаправляет проверенные запросы в модуль `server`. Это внешний API приложения.
*   **`server`** — основной модуль, содержащий всю бизнес-логику приложения. Работает с базой данных, обрабатывает запросы от `gateway` и возвращает ответы.

Директория **`postman`** содержит коллекции тестов Postman, которые использовались для проверки работоспособности API на различных этапах разработки (не является модулем).

## 🛠 Технологический стек

*   **Язык:** Java 21
*   **Фреймворк:** Spring Boot 3, Spring MVC, Spring Data JPA
*   **Сборщик:** Maven (многомодульный проект)
*   **База данных:** PostgreSQL
*   **Миграции:** Схема базы данных управляется через файл `schema.sql` (в модуле `server`)
*   **Контейнеризация:** Docker, Docker Compose
*   **Линтер:** Checkstyle (конфигурация в `checkstyle.xml`)

## 🚀 Запуск проекта

### Предварительные требования

*   Установленные JDK 21 и Maven.
*   Установленный Docker и Docker Compose.

### Вариант 1: Запуск через Docker Compose (рекомендуемый)

Этот способ автоматически собирает образы и запускает все сервисы:

```bash
# Клонируйте репозиторий
git clone https://github.com/VitalyVL1/java-shareit.git
cd java-shareit

# Соберите проект
mvn clean package

# Запустите все сервисы
docker-compose -f compose.yaml up -d
```

После выполнения команды будут запущены три контейнера:
- **db** — PostgreSQL на порту `6541` (хост) → `5432` (контейнер)
- **server** — основной модуль на порту `9090`
- **gateway** — API-шлюз на порту `8080`

API будет доступно по адресу: `http://localhost:8080`

### Вариант 2: Ручной запуск (для разработки)

1. **Запустите базу данных (PostgreSQL) через Docker:**
   ```bash
   docker-compose -f compose.yaml up -d db
   ```
   
2. Соберите проект с помощью Maven:

    ```bash
    mvn clean package
    ```

3. Запустите сервисы:
Сначала запустите модуль server, затем модуль gateway:

    ```bash
    # В папке server
    java -jar server/target/server-*.jar
    
    # В папке gateway (после запуска server)
    java -jar gateway/target/gateway-*.jar
    ```
### Остановка сервисов
Для остановки всех контейнеров выполните:

```bash
docker-compose -f compose.yaml down
```

## 📚 Функциональность
Приложение предоставляет REST API для управления следующими сущностями:

- Пользователи (/users): Создание, просмотр, обновление и удаление пользователей.
- Вещи (/items): Добавление новых вещей, их редактирование, просмотр по ID и поиск по названию/описанию.
- Бронирование (/bookings): Создание запросов на аренду вещи, подтверждение или отклонение бронирования владельцем, просмотр статуса бронирований.
- Запросы вещей (/requests): Создание запросов на вещи, которыми пользователи хотели бы воспользоваться, просмотр своих и чужих запросов.

## 🧪 Тестирование

Приложение имеет высокое качество кода, подтвержденное комплексным тестированием:

### Модульное тестирование (Unit-тесты)

*   **`gateway` модуль**:
    *   Клиенты тестируются через `@RestClientTest` для изоляции клиентской логики
    *   DTO проверяются с помощью `@JsonTest` для гарантии правильной сериализации/десериализации JSON

*   **`server` модуль**:
    *   Мапперы и утилиты покрываются отдельными unit-тестами

### Интеграционное тестирование

*   **Тестирование веб-слоя** — контроллеры тестируются с помощью `@WebMvcTest` с поднятием только MVC-контекста
*   **Тестирование сервисов с базой данных** — сервисы проверяются через `@DataJpaTest`, что позволяет тестировать их в связке с реальными JPA-репозиториями и встроенной БД (интерфейсы репозиториев не требуют отдельного тестирования, так как их реализация генерируется динамически Spring Data JPA)
*   **Проверка загрузки контекста** — главные классы приложений тестируются через `@SpringBootTest` для гарантии, что весь контекст поднимается корректно:
    ```java
    @SpringBootTest
    class ShareItGatewayTests {
        @Test
        void contextLoads() { }
    }
    ```

### Функциональное тестирование API

*   **Postman-коллекции** — в директории [`postman`](./postman) находятся файлы с тестами API, которые проверяют корректность работы всех эндпоинтов приложения. Эти тесты запускаются против работающего приложения и покрывают все основные пользовательские сценарии:
    *   Создание и управление пользователями
    *   Добавление и поиск вещей
    *   Создание и обработка бронирований
    *   Работа с запросами вещей

Такой многоуровневый подход к тестированию с использованием специализированных аннотаций Spring обеспечивает надежность приложения и позволяет безопасно добавлять новый функционал.
